default:
  autoload:
     '': %paths.base%/contexts
  suites:
    default:
      paths: [%paths.base%/features]
      contexts: [FeatureContext]
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost/skola/bp/www
      sessions:
        default:
          sahi: ~
  translation:
    locale: en
